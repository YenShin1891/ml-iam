# Core dependencies for the XGBoost pipeline.
#
# Python 3.9. Note that 3.9 reached end of life in October 2025; the pins below
# are the last releases of each package that still support it.
#
# LSTM/TFT additionally need requirements-advanced.txt.
# Running the tests additionally needs requirements-dev.txt.
#
# These versions are the ones the pipeline is actually run and tested against.

# Core Data Science Libraries
pandas==2.2.2
numpy==1.24.4
scipy==1.13.1
scikit-learn==1.6.1

# Config (used by scripts/train_from_config.py)
pyyaml==6.0.2

# Data serialization (RunStore caches processed data as parquet)
pyarrow>=14.0.0

# XGBoost (GPU is optional: the pipeline runs on CPU without further packages)
xgboost==2.1.4

# Visualization
matplotlib==3.9.4
seaborn==0.13.2

# SHAP (SHapley Additive exPlanations)
shap==0.47.2

# ====================================================================
# DASHBOARD ENVIRONMENT SETUP (due to numpy compatibility issues)
# ====================================================================
# For dashboard/streamlit, create separate environment:
# mamba create -n mliam_st python=3.9
# mamba activate mliam_st
# pip install numpy==1.24.4 pandas==2.0.3 scipy==1.10.1 scikit-learn==1.3.2
# pip install matplotlib==3.7.5 seaborn==0.12.2 numba==0.57.1 shap==0.41.0
# pip install streamlit==1.45.0 xgboost
