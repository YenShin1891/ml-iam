# python version has to be 3.9

# Core Data Science Libraries
pandas==2.2.2
numpy==1.26.4  # streamlit might need numpy 2.0.2 because of dependency issues
scipy==1.10.1  # Compatible with numpy 1.24.4
scikit-learn==1.3.2  # Compatible with numpy 1.24.4 and scipy 1.10.1

# XGBoost with GPU support
xgboost==2.1.4

# Dask and Distributed Computing
dask[dataframe]==2024.8.0
distributed==2024.8.0
# dask-cuda  # Optional: for better NVIDIA GPU integration - commented out due to version conflicts

# Visualization
matplotlib==3.9.4
seaborn==0.13.2 

# SHAP (SHapley Additive exPlanations)
shap==0.47.2

# Streamlit for Web Apps
streamlit==1.45.0

# GPU Support - Fixed package name for Python 3.9
cupy-cuda11x==12.3.0  # Compatible with numpy 1.24.4

# Deep Learning - PyTorch Lightning stack (replaces TensorFlow for LSTM)
torch>=2.0.0
lightning>=2.0.0
# pytorch-forecasting prediction semantics changed after 1.0:
# - 1.4.0: Prediction.output is normalized; Prediction.prediction may be None unless scales are present
# Our pipeline requires original-scale predictions. If using 1.4.x, explicitly denormalize or pin to a version
# that populates Prediction.prediction when calling predict(..., mode="prediction"). See scripts/check_pf_prediction.py
pytorch-forecasting>=0.10.0  # For TFT implementation
