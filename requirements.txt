# Core dependencies for the XGBoost pipeline.
#
# Python 3.9 (end of life October 2025; kept while the published results
# depend on it).
#
# LSTM/TFT additionally need requirements-advanced.txt.
# Running the tests additionally needs requirements-dev.txt.
# The Streamlit dashboard runs in its own environment: requirements-dashboard.txt.
#
# These pins are the environment the results were produced in.

# Core Data Science Libraries
pandas==2.2.2
numpy==1.26.4
scipy==1.10.1
scikit-learn==1.3.2

# Config (used by scripts/train_from_config.py)
pyyaml==6.0.2

# Data serialization (RunStore caches processed data as parquet)
pyarrow>=14.0.0

# XGBoost (GPU is optional: the pipeline runs on CPU without further packages)
xgboost==2.1.4

# Visualization
matplotlib==3.9.4
seaborn==0.13.2

# SHAP (SHapley Additive exPlanations)
shap==0.47.2

# The Streamlit dashboard runs in a separate environment;
# see requirements-dashboard.txt for setup.
