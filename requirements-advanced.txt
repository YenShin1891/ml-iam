# Additional dependencies for LSTM/TFT training, on top of requirements.txt.
#
# These pins are the environment the results were produced in.

-r requirements.txt

# Deep Learning (Optional, Only needed for LSTM/TFT)
torch==2.8.0
lightning==2.6.0

# The TFT code targets the 1.4 API specifically: the Prediction namedtuple and
# its field order, TimeSeriesDataSet.from_parameters, and the shape of
# Output.prediction for multiple targets. Earlier releases differ in all three.
pytorch-forecasting==1.4.0
